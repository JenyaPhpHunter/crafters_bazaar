// Перевіряємо, чи користувач може редагувати цю модель
        if (Gate::allows('edit', $kind_product)) {
            // Користувач має право редагувати
            echo "User can edit this sub-kind product.";
            // Ви можете додати код для редагування тут або просто продовжити виконання контролера
        } else {
            // Користувач не має права редагувати
            echo "User is not authorized to edit this sub-kind product.";
            // Можете перенаправити або виконати інші дії
        }
